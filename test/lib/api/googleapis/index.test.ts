import { expect } from "chai";

describe("GoogleAPIS General Function", () => {
  describe("initGoogleAPIS", () => {
    it("should authorize and return a jwtClient");
    it("should do nothing if an error occur");
  });
});
