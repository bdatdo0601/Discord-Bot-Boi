import { expect } from "chai";

describe("GoogleAPIS Calendar Function", () => {
  describe("createNewCalendar", () => {
    it("should authorize and return a jwtClient");
    it("should throw error if one occur");
  });
  describe("deleteCalendar", () => {
    it("should delete calendar");
    it("should throw error if one occurs");
  });
});
