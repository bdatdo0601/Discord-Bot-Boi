import chai, { expect } from "chai";
import dotenv from "dotenv";
dotenv.config();

describe("Application Test", () => {
  it("should run the application and can be destroyed", (done) => {
    done();
  });
});
